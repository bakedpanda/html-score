services:
  score-bug:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ${DATA_DIR:-./data}:/app/data
      - ${UPLOADS_DIR:-./public/uploads}:/app/public/uploads
    restart: unless-stopped
